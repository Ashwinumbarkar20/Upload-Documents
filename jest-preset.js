module.exports = {
    // Define Jest presets or configurations here
    preset: 'ts-jest', // Example preset (if using TypeScript)
    transform: {
      '^.+\\.jsx?$': 'babel-jest', // Transform JSX files using Babel
    },
    // Add any other Jest configuration options as needed
  };